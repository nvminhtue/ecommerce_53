<div class="row">
  <div class="col-lg-12">
      <h1 class="page-header"><%= t ".mana_user" %></h1>
  </div>
</div>
<!-- /title -->
<!-------------- Table ----------------->
<div class="row">
<div class="col-sm-12">
<div class="panel panel-default mytable">
  <div class="panel-heading">
    <%= t ".title_table" %>
  </div>
<!-- /.panel-heading -->
<div class="panel-body">
<div class="table-responsive">
  <table class="table table-striped table-bordered table-hover">
    <tr>
      <% columns = load_user_columns %>
      <% columns.each do |col| %>
        <th><%= col.capitalize %></th>
      <% end %>
      <th width="80px">EDIT</th>
      <th width="100px">DELETE</th>
    </tr>
    <tr>
    <!-- table user -->
    <% @users.each do |u| %>
    <tr>
      <% for i in 0...columns.count do %>
      <td><%= u[columns[i]] %></td>
      <% end %>
      <td class="table-update">
        <%= link_to edit_admin_user_path u do %>
          <i class="fa fa-pencil-square-o" aria-hidden="true"></i> EDIT
        <% end %>
      </td>
      <td class="table-delete">
        <%= link_to admin_user_path(u), method: :delete, data: {confirm: "You sure?"} do %>
           <i class="fa fa-trash-o" aria-hidden="true"></i> DELETE
        <% end %>
      </td>
    </tr>
    <% end %>
    <!-- /table user -->
  </table>
<div>
  <%= link_to t(".add_new"), new_admin_user_path %>
</div>
</div>
  <%= will_paginate @users %>
</div>
</div>
</div>
</div>
