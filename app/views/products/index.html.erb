
<% provide :title, t(".title") %>
<h1><%= t ".title" %></h1>

<%= will_paginate %>
  <% @products.each do |product| %>
  <%= link_to (image_tag product.picture.url), product %>
  <%= render "product_row", product: product%>
  <% end %>
<%= will_paginate %>
